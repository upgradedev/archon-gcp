# Dev / CI tooling — test pyramid, notebook execution, dependency audit.
-r requirements.txt
pytest>=8.0.0
pytest-cov>=5.0.0
nbmake>=1.5.0          # executes notebook.ipynb top-to-bottom in CI
pip-audit>=2.7.0       # dependency vulnerability scan
