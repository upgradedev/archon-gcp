# Core bookkeeping + tests — Python stdlib only, plus pytest.
pytest>=8.0.0

# Conversational agent (optional: python -m archon.cli --agent)
google-adk>=1.0.0

# GCP-native persistence (optional: set GOOGLE_CLOUD_PROJECT)
google-cloud-firestore>=2.16.0
google-cloud-storage>=2.16.0
